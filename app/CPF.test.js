const CPF = require("./CPF")

describe('CPF', () => {
  describe('construtor', () => {
    it('deve criar uma instância válida de CPF com um número válido de CPF', () => {
      // preenche aí
    })

    it('deve lançar um erro para um número inválido de CPF', () => {
      // preenche aí
    })
  })

  describe('equals', () => {
    it('deve retornar true quando compara duas instâncias de CPF com números iguais', () => {
      // preenche aí
    })

    it('deve retornar false quando compara duas instâncias de CPF com números diferentes', () => {
      // preenche aí
    })
  })

  describe('toString', () => {
    it('deve retornar a representação do CPF numa string', () => {
      // preenche aí
    })
  })
})